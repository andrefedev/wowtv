import 'package:flutter/material.dart';

class HomeSeriePage extends StatelessWidget {
  const HomeSeriePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home Serie Page"),
      ),
    );
  }
}
